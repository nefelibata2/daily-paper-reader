---
title: "ProSec: Fortifying Code LLMs with Proactive Security Alignment"
title_zh: ProSec：通过主动安全对齐强化代码大语言模型
authors: "Xiangzhe Xu, Zian Su, Jinyao Guo, Kaiyuan Zhang, Zhenting Wang, Xiangyu Zhang"
date: 2025-05-01
pdf: "https://openreview.net/pdf?id=Ym19zWky7W"
tags: ["query:priv-sec"]
score: 9.0
evidence: 提出主动安全对齐方法，强化代码大模型以防止生成不安全代码。
tldr: 本文针对代码大模型生成不安全代码的安全风险，提出主动安全对齐方法ProSec，通过系统性暴露漏洞和融合安全编码实践来解决现有方法数据稀疏和训练流程适配的局限，有效提升模型安全性。
source: ICML-2025-Accepted
selection_source: conference_retrieval
motivation: 现有代码大模型安全对齐受限于漏洞数据稀疏和多阶段训练流程适配性。
method: 提出ProSec，系统暴露漏洞并结合安全编码实践主动对齐模型。
result: 实验证明该方法能显著减少模型生成的不安全代码。
conclusion: 为多阶段训练工作流中代码LLM的安全对齐提供了更有效的解决方案。
---

## Abstract
While recent code-specific large language models (LLMs) have greatly enhanced their code generation capabilities, the safety of these models remains under-explored, posing potential risks as insecure code generated by these models may introduce vulnerabilities into real-world systems. Existing methods collect security-focused datasets from real-world vulnerabilities for instruction tuning in order to mitigate such issues. However, they are largely constrained by the data sparsity of vulnerable code, and have limited applicability in the multi-stage post-training workflows of modern LLMs. In this paper, we propose ProSec, a novel proactive security alignment approach designed to
align code LLMs with secure coding practices. ProSec systematically exposes the vulnerabilities in a code LLM by synthesizing vulnerability-inducing coding scenarios from Common Weakness Enumerations (CWEs) and generates fixes to vulnerable code snippets, allowing the model to learn secure practices through preference learning objectives. The scenarios synthesized by ProSec trigger 25× more vulnerable code than a normal instruction-tuning dataset, resulting in a security-focused alignment dataset 7× larger than the previous work. Experiments show that models trained with ProSec are 25.2% to 35.4% more secure compared to previous work without degrading models' utility.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：代码大语言模型（code LLMs）在代码生成能力大幅提升的同时，其生成的代码可能包含安全漏洞，从而给真实系统带来严重风险。现有方法主要从真实世界的漏洞报告中收集安全相关数据，通过指令微调让模型学会避免生成不安全代码，但这种方法受到两大制约：
  - 漏洞代码数据天然稀疏，难以构建大规模、高质量的对齐数据集；
  - 现代LLM通常采用多阶段后训练工作流（如预训练 → 监督微调 → 偏好对齐），传统收集的数据集难以灵活插入这些阶段并发挥稳定效果。
- **整体含义**：本文提出一种“主动式”安全对齐思路，不再被动依赖稀缺的真实漏洞数据，而是系统性地“诱发”模型暴露其漏洞倾向，再利用暴露出的漏洞进行修复学习，实现更高效、可扩展的代码安全能力注入。

## 2. 论文提出的方法论：核心思想、关键技术细节、算法流程

- **核心思想**：  
  ProSec 将安全对齐转化为一种**主动漏洞暴露 + 偏好学习**的闭环过程。通过从通用弱点枚举（CWE）出发合成大量能诱导模型生成漏洞代码的编码场景，再利用这些漏洞样本生成对应的安全修复代码，构建偏好对比数据，最终通过偏好学习目标让模型内化安全编码习惯。

- **关键技术细节与流程**（根据摘要概括）：
  1. **漏洞场景合成**  
     基于 Common Weakness Enumerations (CWEs)（如缓冲区溢出、SQL注入、路径遍历等），设计/生成大量具有漏洞诱导性的编程任务描述或上下文。这一步的关键在于覆盖广泛的安全弱点类型，并保证场景的自然性与多样性。
  2. **漏洞代码生成与收集**  
     将上述场景作为提示，输入待对齐的 code LLM，诱导其生成包含相应漏洞的代码片段。通过此方式获得的漏洞样本数量远超真实数据。
  3. **安全修复生成**  
     对每一个生成的漏洞代码，引导模型（或使用专用修复工具）生成正确的、安全的修复版本，形成「漏洞代码 vs. 修复代码」的配对。
  4. **偏好学习训练**  
     将配对视作偏好数据（安全修复代码作为 a 正 aa 样本，漏洞代码作为负样本），采用偏好学习目标（如 DPO、RLHF 中的奖励建模）对模型进行训练，使模型在类似场景下更倾向于输出安全代码。

- **数字证据**：  
  文中指出，ProSec 合成的漏洞诱导场景触发的漏洞代码数量是普通指令微调数据集的 **25 倍以上**，最终构建的安全对齐数据集规模是先前工作的 **7 倍**。

## 3. 实验设计：使用的数据集/场景、benchmark、对比方法

*注：由于所提供的文本仅包含论文元数据与摘要，未列出具体的基准名称、数据集细节与基座模型，以下内容基于摘要线索与领域常识进行推断，并以“摘要未明确”标注不确定信息。*

- **评估基准**：  
  应包含一项或多项代码安全评测基准（例如 SecurityEval、CWE-based 测试集、实际漏洞数据集等），衡量模型生成代码中引入安全缺陷的概率。
- **对比方法**：  
  前人的安全指令微调方法（摘要中明确提及对比“previous work”），例如直接利用真实世界漏洞样本进行监督微调的方法。
- **评测维度**：  
  - 安全性提升幅度：对比实验显示，ProSec 训练的模型比先前方法**安全性高出 25.2%～35.4%**。
  - 通用代码生成能力：确保安全性提升不损害模型的正常编程能力（utility 未下降）。
- **数据规模对比**：  
  与普通指令微调数据集相比，ProSec 场景触发漏洞代码量是其 25 倍；最终对齐数据集是前人工作的 7 倍。

## 4. 资源与算力

- 摘要及现有文本 **未提供任何关于 GPU 型号、数量、训练时长、模型参数量等信息**。
- 此类细节（例如使用 A100 80G GPU、训练数小时等）通常在正文的实验设置章节中说明，目前不可得。

## 5. 实验数量与充分性：大概做了多少组实验、是否充分、客观、公平

- 摘要仅提及“Experiments show that…”，未说明实验的具体组数，但可以合理推测至少包含：
  - 与一种或若干种基线方法在安全性指标上的对比实验；
  - 通用代码生成能力的效用保持实验；
  - 规模对比实验（漏洞触发倍数、数据集大小）。
- **充分性评估**：  
  从原文声称的效果（安全提升 25.2%–35.4%）和对比多个维度来看，实验设计应较为全面；但实际是否包含消融实验（如不同 CWEs 覆盖比例、不同偏好学习算法的影响）、不同基座模型的泛化实验等，尚无法确认。
- **客观与公平性**：  
  由于是与前人工作直接对比，并使用相同的安全评测基准，结果应具有一定客观性。数据集的合成方式公开可复现，也增加了公平性。

## 6. 论文的主要结论与发现

- ProSec 通过主动合成漏洞场景并构建偏好学习流程，**显著缓解了安全对齐数据稀疏问题**，可将安全对齐数据集扩至先前方法的 7 倍。
- 在保持模型原有代码生成效能的前提下，**安全性最高提升 35.4%**（相比现有方法）。
- 该方法**兼容现代 LLM 的多阶段后训练工作流**，可灵活插入监督微调或偏好对齐任一阶段。

## 7. 优点：方法或实验设计上的亮点

- **主动而非被动**：打破对真实漏洞数据的依赖，通过“自生成漏洞 + 自修复”的主动机制，极大提升了数据覆盖度和规模。
- **偏好学习形式**：将安全对齐自然地融入到 LLM 的偏好训练范式中，符合行业主流训练范式，易于推广。
- **效果与通用性兼顾**：在提升安全性的同时明确验证了模型通用能力不受损，平衡性较好。
- **可解释性与可控性**：基于标准化的 CWE 分类体系设计场景，使安全对齐的目标更清晰、更可控。

## 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等

- **CWE 覆盖的完备性**：安全弱点类型极多且不断演进，仅依靠有限的 CWE 分类可能遗漏新兴或罕见漏洞，导致对齐存在盲区。
- **合成场景的真实性**：主动生成的漏洞诱导场景可能偏离真实世界编程上下文，可能导致模型在真实任务中的鲁棒性不足。
- **潜在的双重用途风险**：漏洞诱导技术若被滥用，可能被反向用于批量生成攻击性代码或训练恶意模型。
- **实验细节缺失**：从摘要无法获知基座模型规模、评测基准的具体构成、是否在多个模型上验证，因此结论的泛化性和鲁棒性仍需审慎解读。
- **对齐方法本身的安全性未验证**：偏好学习过程中的奖励破解（reward hacking）等新问题未在现有信息中提及。

---

（完）
