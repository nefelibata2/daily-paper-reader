---
title: On the Inseparability of Instructions and Data in Shared-Embedding Sequence Models
title_zh: 共享嵌入序列模型中指令与数据的不可分离性
authors: "Dewank Pant, Shruti Lohani, Avijit Kumar"
date: 2026-06-25
pdf: "https://arxiv.org/pdf/2606.27567v1"
tags: ["query:priv-sec"]
score: 9.0
evidence: 证明了在共享嵌入模型中完美提示注入防护的不可实现性，揭示了LLM的根本安全漏洞。
tldr: LLM集成应用面临严重提示注入风险，现有防御屡遭突破并非巧合。本文通过形式化证明，在共享嵌入且无强制控制-数据分离的架构中，完美防护在数学上不可能实现。三个定理（出处不可恢复、控制路径暴露、有限覆盖不变性间隙）揭示了根本缺陷。该结构性问题类似冯·诺依曼机缓冲溢出，要求指令与数据通道架构分离，而非仅在流水线内优化。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.27567v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1218, \"height\": 613, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.27567v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1566, \"height\": 509, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27567v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1041, \"height\": 260, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27567v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 579, \"height\": 353, \"label\": \"Table\"}]"
motivation: 现有LLM应用的提示注入防御均遭突破，背后原因并非巧合，而是共享嵌入架构缺乏控制-数据分离的根本缺陷。
method: 通过定义Prompted Action Models与语义忠实控制(SFC)，并证明出处不可恢复、控制路径暴露、有限覆盖不变性间隙三个不可能结果。
result: 结果表明，在共享嵌入架构下，完美提示注入防护在数学上不可能实现，该缺陷与冯·诺依曼机缓冲溢出同源，属于结构性问题。
conclusion: 提示注入无法仅靠流水线内分类或对齐根除，必须从根本上分离指令与数据通道，此即解决方案类别的需求。
---

## 摘要
提示注入是集成LLM应用面临的首要安全风险，然而迄今为止提出的每一种防御措施都已被攻破。我们证明这并非巧合：在缺乏强制控制-数据分离的共享嵌入架构中，完美的提示注入预防在数学上是不可能的。我们将提示系统形式化为提示动作模型，其输出包括控制权威动作：拒绝决策、工具授权、策略路由和记忆写入。我们定义了语义忠实控制（SFC），即此类行为仅依赖于不可信输入的意义，而非其编码方式。然后，我们通过三个结果证明在共享流水线中实现SFC是不可行的：来源恢复不可能性（共享表示使可信与不可信内容在统计上不可区分，由总变分距离界定）；控制路径暴露（不可信令牌通过决定输出的同一个注意力值聚合进入控制相关计算）；以及有限覆盖不变性差距（有限训练无法在无限的语义等价类上验证不变性）。我们在生产级分词器和模型上测量了每个量。该结果是结构性的，并非当前防御的缺陷。它反映了冯·诺依曼机中代码-数据混淆导致缓冲区溢出的现象，这类漏洞需要数十年分层防御（数据执行保护、写异或执行、地址空间布局随机化、栈金丝雀，并最终采用内存安全语言）才得以遏制，因为没有任何单一机制足够。其含义是相同的：仅靠更好的流水线内分类或对齐无法消除提示注入，它要求指令与数据通道在架构上分离。我们确定了根本原因及其所需的解决方案类别。

## Abstract
Prompt injection is the top security risk for LLM-integrated applications, yet every defense proposed so far has been broken. We prove this is not a coincidence: in shared-embedding architectures that lack enforced control-data separation, perfect prompt-injection prevention is mathematically impossible. We formalize prompted systems as Prompted Action Models whose outputs include control-authoritative actions: refusal decisions, tool authorization, policy routing, and memory writes. We define Semantic-Faithful Control (SFC), the property that such behavior depends only on the meaning of untrusted input, not on how it is encoded. We then prove SFC is unachievable within the shared pipeline, via three results: a provenance-recovery impossibility (shared representations make trusted and untrusted content statistically inseparable, bounded by total variation distance); control-path exposure (untrusted tokens enter control-relevant computation through the same attention value-aggregation that determines outputs); and a finite-coverage invariance gap (finite training cannot certify invariance over infinite semantic-equivalence classes). We ground each quantity in measurements on production tokenizers and models.   The result is structural, not a gap in current defenses. It mirrors the code-data confusion in Von Neumann machines that gives rise to buffer overflows, a vulnerability class that took decades of layered defenses (DEP, Write-XOR-Execute, ASLR, stack canaries, and ultimately memory-safe languages) to contain, because no single mechanism sufficed. The implication is the same: prompt injection cannot be eliminated by better in-pipeline classification or alignment alone. It requires architectural separation of instruction and data channels. We identify the root cause and the class of solution it demands.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义
- **核心问题**：共享嵌入架构下的大语言模型（LLM）在面对提示注入攻击时，是否存在完美预防的理论可能？
- **研究动机与背景**：当前各类提示注入防御（指令微调、RLHF、护栏分类器、困惑度过滤、电路断路器等）均被攻破，多轮攻击成功率达90–99%。这促使研究者追问：这究竟是防御方法的不足，还是架构本身存在根本缺陷？
- **整体含义**：本文给出形式化证明——在缺乏强制控制‑数据分离的共享嵌入架构中，完美提示注入预防在数学上不可能实现。这一结果与冯·诺依曼机中代码‑数据混淆导致缓冲区溢出漏洞的结构性缺陷类似，意味着仅靠流水线内的分类或对齐优化无法根除此问题，必须从架构层面分离指令与数据通道。

## 2. 论文提出的方法论
- **核心思想**：将提示系统形式化为“提示动作模型”，定义安全属性“语义忠实控制”，然后通过三个独立的不可能性结果，证明在共享表示流水线内无法保证 SFC。
- **关键技术细节**：
    - **提示动作模型** $M = (\Sigma, d, E, f, A)$：共享嵌入函数 $E$ 将可信指令 $s$ 和不可信内容 $u$ 映射到同一空间；模型 $f$ 输出动作分布，包括控制权威动作（拒绝、工具授权、策略路由、记忆写入）。
    - **语义忠实控制（SFC）**：要求控制权威行为仅依赖于不可信输入的**语义内容**，而非语义无关的编码变化（如字符混淆、Base64 编码、空白后缀等）。即若 $u \equiv_{\text{sem}} u'$，则两者的投影控制输出相等。
    - **三个不可能结果**：
        1. **出处恢复不可能**（定理3）：由于共享词汇嵌入重叠（引理1），可信与不可信表示的条件分布总变分距离 $TV < 1$，最优出处分类错误率 $P_e^\star = \frac12(1 - TV) > 0$。
        2. **控制路径暴露**（定理4）：不可信内容通过注意力值聚合进入控制相关节点，且总注意力质量非零（对任何有用的模型必然成立）。
        3. **有限覆盖不变性差距**（引理5）：有限训练集无法覆盖无限或超多项式增长的语义等价类，故无法验证对所有编码变体的不变性。
    - **主定理**（定理6）：结合上述三点，证明在仅靠共享流水线内学习的机制下，无法保证对所有输入满足 SFC，即完美防护不可能。
    - **桥梁命题**（命题8）：在非退化假设下，表示层面的语义无关扰动会以概率1导致行为变化。

## 3. 实验设计
- **数据集与场景**：
    - 指令语料：合并自四个公开系统提示仓库（TheBigPromptLibrary、jujumilk3、YeeKal、asgeirtj）。
    - 用户数据语料：UltraChat‑200K 中的 50,000 条用户轮次。
    - 分词器覆盖 Mistral‑7B (32K)、Llama‑3.1 (128K)、Qwen‑2.5 (152K)。
- **实验内容（4 项测量）**：
    1. **词汇重叠度**：计算类型重叠 Jaccard、概率质量重叠、指令语料被覆盖比例 $\rho_{\text{cov}}$。
    2. **表示层重叠**：在 Llama‑3.1‑8B‑Instruct 上，计算 0、8、16、24、31 层隐藏状态的最大均值差异（MMD），用 bootstrap 95% CI。
    3. **行为 SFC 违背**：金融系统提示 + 基线投资查询，前面加入 5 种注入技术（DAN、多示例、伪造系统覆盖、渐进式、虚拟化劫持），测试 Llama‑3.1‑8B‑Instruct 和 Mistral‑7B‑Instruct‑v0.2 的输出变化。
    4. **权重手术确认（消融）**：引用已有的“抑制”（abliteration）实验结果作为经验佐证（非正式证据）。
- **对比方法**：并非直接对比防御方法，而是通过几何和行为测量来验证理论的三个前提（词汇重叠、表示不可分、控制路径暴露）。

## 4. 资源与算力
- 论文**未明确报告** GPU 型号、数量及训练时长。实验部分主要为离线测量（计算词汇重叠、MMD、模型推理），使用的模型为 8/7B 参数级别，推理采用 8‑bit 量化，对算力需求较小，但具体算力未提及。

## 5. 实验数量与充分性
- **实验数量**：共进行了 4 类测量（词汇重叠 ×3 分词器，表示层 MMD ×5 层，行为注入 ×2 模型 ×5 攻击，外加引用消融实验）。总体样本量较小（表示层每组 300 序列），但采用 bootstrap 置信区间，主要作为理论假设的**经验落地验证**，而非大规模基准。
- **充分性与客观性**：实验瞄准论文形式化定理中的三个核心量（词汇重叠、表示分布重叠、控制路径影响），设计合理且针对性强。但缺乏多模型、多尺寸、多提示模板的全面对比，也缺少人为可控的消融实验（如不同防御下的行为变化）。不过，鉴于论文性质为理论不可能性证明，实验部分主要用于消除“假设是否变态”的疑虑，因此可认为是充分的。

## 6. 论文的主要结论与发现
- 在共享嵌入且无强制出处屏障的架构中，**完美提示注入防护在数学上不可能**，根源在于表示空间内的不可区分性。
- 出处恢复不可能性（基于总变分距离）、控制路径暴露（不可信值必定流入控制计算）、有限训练无法覆盖无限等价类，三者共同导致任何纯流水线内防御必然存在漏洞。
- 该缺陷是**架构性**的，与冯·诺依曼机中代码‑数据混淆同质，暗示未来需要诸如指令/数据分离表示、不可伪造出处通道等结构性方案，而非仅靠行为对齐。
- 位置编码、分段标记等软性机制不足以提供不可变保障，因为它们本身是可伪造的。

## 7. 优点
- **理论深度与原创性**：首次给出提示注入完美预防的 impossibility 定理，将问题从工程漏洞提升至架构约束层面。
- **严谨的形式化**：定义清晰（提示动作模型、SFC），证明链条完整（引理、定理、桥梁命题），覆盖代表性攻击类（GCC 后缀、间接注入等）。
- **结构类比深刻**：与冯·诺依曼机类比有理有据，提供了清晰的历史参考和发展路线图。
- **经验验证配合恰当**：通过词汇重叠、MMD、行为注入和权重手术四项实测，将理论假设与真实模型状态对应，增强说服力。
- **边界明确**：明确列出定理适用范围（共享流水线、无不可变出处屏障），并给出跳出定理的架构范例（分离编码器、硬遮罩等），避免过度泛化。

## 8. 不足与局限
- **实验规模较小**：仅重点测量了两个模型（Llama‑3.1‑8B、Mistral‑7B），且行为实验中未出现拒绝决策直接翻转（输出长度、前言等改变），对“直接控制权威输出违背”的实证支撑较弱（主要依靠引用 GCG 和消融）。
- **依赖非退化假设**：假设1–2要求所学权重不是病态（不会将所有对抗扰动严格湮灭），虽在现有模型上合理，但无法从理论上排除未来可能出现的极端鲁棒模型。
- **有限覆盖的假设严格性**：引理5要求语义等价类无限或超多项式增长，某些应用下等价类可能较小（如仅限格式化差异），此时“完美预防”的障碍会减弱，但定理未深入讨论这种情况下的量化影响。
- **未涉及训练数据依存**：证明未明确讨论训练分布可能包含大量对抗样本进而使不变性被极近似逼近的可能性（虽引理5已指出无法完全覆盖，但实际风险可能极低）。
- **实践指导性有限**：论文给出了架构分离的解决方向，但未提供具体实现代价或安全性‑效用权衡分析。

（完）
